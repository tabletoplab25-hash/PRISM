<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PRISM - Strategic Color Card Game</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            max-width: 900px;
            width: 100%;
        }

        .logo-section {
            text-align: center;
            margin-bottom: 50px;
            animation: fadeInDown 0.8s ease-out;
        }

        .logo {
            font-size: 5em;
            margin-bottom: 20px;
            text-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        h1 {
            font-size: 4em;
            color: white;
            letter-spacing: 15px;
            margin-bottom: 10px;
            text-shadow: 0 5px 20px rgba(0,0,0,0.3);
        }

        .tagline {
            color: rgba(255,255,255,0.9);
            font-size: 1.3em;
            font-weight: 300;
        }

        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            margin-bottom: 40px;
        }

        .menu-card {
            background: white;
            border-radius: 20px;
            padding: 40px 30px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            transition: all 0.3s ease;
            cursor: pointer;
            text-decoration: none;
            color: inherit;
            display: block;
            animation: fadeInUp 0.8s ease-out;
        }

        .menu-card:nth-child(1) { animation-delay: 0.1s; }
        .menu-card:nth-child(2) { animation-delay: 0.2s; }
        .menu-card:nth-child(3) { animation-delay: 0.3s; }

        .menu-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 40px rgba(0,0,0,0.4);
        }

        .menu-icon {
            font-size: 4em;
            margin-bottom: 20px;
            display: block;
        }

        .menu-card h2 {
            color: #667eea;
            font-size: 1.8em;
            margin-bottom: 15px;
        }

        .menu-card p {
            color: #666;
            font-size: 1em;
            line-height: 1.6;
        }

        .color-strip {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-top: 50px;
            animation: fadeIn 1s ease-out 0.5s both;
        }

        .color-dot {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }

        .color-dot.red { background: linear-gradient(135deg, #e74c3c, #c0392b); }
        .color-dot.blue { background: linear-gradient(135deg, #3498db, #2980b9); }
        .color-dot.green { background: linear-gradient(135deg, #2ecc71, #27ae60); }
        .color-dot.yellow { background: linear-gradient(135deg, #f1c40f, #f39c12); }
        .color-dot.purple { background: linear-gradient(135deg, #9b59b6, #8e44ad); }

        .footer {
            text-align: center;
            color: rgba(255,255,255,0.7);
            margin-top: 40px;
            font-size: 0.9em;
            animation: fadeIn 1s ease-out 0.7s both;
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2.5em;
                letter-spacing: 8px;
            }

            .logo {
                font-size: 3em;
            }

            .tagline {
                font-size: 1em;
            }

            .menu-grid {
                grid-template-columns: 1fr;
            }

            .menu-card {
                padding: 30px 20px;
            }
        }

        /* Hover effect for color dots */
        .color-dot {
            animation: pulse 2s ease-in-out infinite;
        }

        .color-dot:nth-child(1) { animation-delay: 0s; }
        .color-dot:nth-child(2) { animation-delay: 0.2s; }
        .color-dot:nth-child(3) { animation-delay: 0.4s; }
        .color-dot:nth-child(4) { animation-delay: 0.6s; }
        .color-dot:nth-child(5) { animation-delay: 0.8s; }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.1);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo-section">
            <div class="logo">🎴</div>
            <h1>PRISM</h1>
            <p class="tagline">A Strategic Color Card Game</p>
        </div>

        <div class="menu-grid">
            <a href="prism-game.html" class="menu-card">
                <span class="menu-icon">🎮</span>
                <h2>Play Game</h2>
                <p>Launch the web app and start playing PRISM online. 2-6 players supported.</p>
            </a>

            <a href="prism-rulebook.html" class="menu-card">
                <span class="menu-icon">📖</span>
                <h2>Rulebook</h2>
                <p>Learn how to play with complete rules, examples, and strategy tips.</p>
            </a>

            <a href="prism-reference.html" class="menu-card">
                <span class="menu-icon">🎴</span>
                <h2>Quick Reference</h2>
                <p>Print-friendly reference card with color abilities at a glance.</p>
            </a>
        </div>

        <div class="color-strip">
            <div class="color-dot red"></div>
            <div class="color-dot blue"></div>
            <div class="color-dot green"></div>
            <div class="color-dot yellow"></div>
            <div class="color-dot purple"></div>
        </div>

        <div class="footer">
            <p>PRISM © 2025 | A game of colors, strategy, and calculated risks</p>
        </div>
    </div>
</body>
</html>
